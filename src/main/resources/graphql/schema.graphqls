
type Query {
    books(bookIds: [Int]): [Book]
}

type Book {
    bookId: Int
    name: String
    pageCount: Int
    author: Author
    qtySold: Int
    rating: Int
}

type Author {
    authorId: Int
    firstName: String
    lastName: String
}